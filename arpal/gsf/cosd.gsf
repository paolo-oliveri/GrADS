#-- torna il coseno di un angolo espresso in gradi
#-- dvd 2020/01
function cosd(expr)
 if(expr='help' | expr='expr' | expr='')
  say 'uso: cosd(expr)'
  say 'torna il coseno di un angolo espresso in gradi'
  say
  return
 endif
return 'cos('expr'*3.1414/180)'
