#-- torna il seno di un angolo espresso in gradi
#-- dvd 2020/01
function sind(expr)
 if(expr='help' | expr='expr' | expr='')
  say 'uso: sind(expr)'
  say 'torna il seno di un angolo espresso in gradi'
  say
  return
 endif
return 'sin('expr'*3.1414/180)'
